import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bodyText: {
        // fontFamily: 'proxima' T0D0 - throws error
    },
    title: {
        // fontFamily: 'proxima' TODO - throws error
        fontSize: 18
    }
});